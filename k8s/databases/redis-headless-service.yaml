apiVersion: v1
kind: Service
metadata:
  name: redis-headless
  namespace: rag-platform
  labels:
    app: redis
spec:
  clusterIP: None
  ports:
    - port: 6379
      name: redis
  selector:
    app: redis
